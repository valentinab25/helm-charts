apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: www-eionet-data
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: www-eionet-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

