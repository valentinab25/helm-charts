apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eionet-ldapdump
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: eionet-ldapdump
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi

