apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "uns.labels" . | nindent 4 }}
    component: configdir
  name: {{ .Release.Name }}-configdir
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
