apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "appl.labels" . | nindent 4 }}
  name: {{ .Release.Name }}

spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
