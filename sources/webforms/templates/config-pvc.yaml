apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "webforms.labels" . | nindent 4 }}
    component: config
  name: {{ .Release.Name }}-config
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
