apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "webforms.labels" . | nindent 4 }}
    component: database
  name: {{ .Release.Name }}-database
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
