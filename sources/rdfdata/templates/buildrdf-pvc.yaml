apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "rdfdata.labels" . | nindent 4 }}
    component: buildrdf
  name: {{ .Chart.Name }}-buildrdf
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
