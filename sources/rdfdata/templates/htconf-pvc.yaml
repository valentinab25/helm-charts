apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "rdfdata.labels" . | nindent 4 }}
    component: htconf
  name: {{ .Chart.Name }}-htconf
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
