apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "rdfdata.labels" . | nindent 4 }}
    component: htdocs
  name: {{ .Chart.Name }}-htdocs
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
