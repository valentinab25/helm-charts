apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: htdocs
  name: {{ .Release.Name }}-htdocs
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
