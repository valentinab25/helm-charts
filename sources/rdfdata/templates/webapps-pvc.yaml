apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: webapps
  name: {{ .Release.Name }}-webapps
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
