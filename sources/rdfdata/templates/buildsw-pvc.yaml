apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "rdfdata.labels" . | nindent 4 }}
    component: buildsw
  name: {{ .Chart.Name }}-buildsw
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
