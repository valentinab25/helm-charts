apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-dpl-plone
  labels:
    {{- include "appl.labels" . | nindent 4 }}
    component: plone

spec:
  replicas: {{ .Values.plone.replicaCount }}
  selector:
    matchLabels:
      {{- include "appl.selectorLabels" . | nindent 6 }}
      component: plone

  template:
    metadata:
      labels:
        {{- include "appl.selectorLabels" . | nindent 8 }}
        component: plone
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "appl.serviceAccountName" . }}
      containers:
      - name: plone
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        env:
        - name: SERVER_NAME
          value: "{{ .Values.plone.hostname }}"
        - name: SENTRY_SITE
          value: "{{ .Values.plone.hostname }}"
        - name: GRAYLOG_FACILITY
          value: "{{ .Values.plone.hostname }}"
        - name: BACKEND_VERSION
          value: "{{ .Values.image.tag | default .Chart.AppVersion }}"
        - name: SENTRY_RELEASE
          value: "{{ .Values.image.tag | default .Chart.AppVersion }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        - name: RELSTORAGE_DSN
          value: "host='postgres' dbname='{{ .Values.plone.database.POSTGRES_DB }}' user='{{ .Values.plone.database.POSTGRES_USER }}' password='{{ .Values.plone.database.POSTGRES_PASSWORD }}'"
        {{- range $key, $value := .Values.plone.environment }}
        {{- if ($value) }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- end }}

        ports:
        - name: http
          containerPort: 8080

        {{- if (.Values.plone.livenessProbe) }}
        livenessProbe:
          httpGet:
            path: {{ .Values.plone.livenessProbe.httpGet.path }}
            port: http
          initialDelaySeconds: {{ .Values.plone.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.plone.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.plone.livenessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.plone.livenessProbe.successThreshold }}
          failureThreshold: {{ .Values.plone.livenessProbe.failureThreshold }}
        {{- end }}

        {{- if (.Values.plone.startupProbe) }}
        startupProbe:
          httpGet:
            path: {{ .Values.plone.startupProbe.httpGet.path }}
            port: http
          initialDelaySeconds: {{ .Values.plone.startupProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.plone.startupProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.plone.startupProbe.timeoutSeconds }}
          successThreshold: {{ .Values.plone.startupProbe.successThreshold }}
          failureThreshold: {{ .Values.plone.startupProbe.failureThreshold }}
        {{- end }}

        {{- if (.Values.plone.readinessProbe) }}
        readinessProbe:
          httpGet:
            path: {{ .Values.plone.readinessProbe.httpGet.path }}
            port: http
          initialDelaySeconds: {{ .Values.plone.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.plone.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.plone.readinessProbe.timeoutSeconds }}
          successThreshold: {{ .Values.plone.readinessProbe.successThreshold }}
          failureThreshold: {{ .Values.plone.readinessProbe.failureThreshold }}
        {{- end }}
                   
        resources:
          {{- toYaml .Values.plone.resources | nindent 10 }}

      {{- with .Values.plone.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.plone.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.plone.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Always

