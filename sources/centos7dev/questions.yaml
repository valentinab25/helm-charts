questions:
- variable: networkSecurity.enabled
  type: boolean
  default: true
  description: Only allow pod to talk to other pods in same namespace
  label: Block pod from accessing the Internet

- variable: mounts
  type: multiline
  default: []
  description: PVCs of volumes to mount
  label: Enter the names of the Persistent Volume Claims. They will be mounted under /mnt

- variable: timezone
  type: string
  default: Europe/Copenhagen
  label: Time zone
